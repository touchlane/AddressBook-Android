<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="searchViewStyle">@style/AppTheme.SearchView</item>
    </style>

    <style name="AppTheme.SearchView" parent="Widget.AppCompat.SearchView.ActionBar">
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

    <style name="Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="TextSection">
        <item name="android:paddingLeft">@dimen/padding_large_16</item>
        <item name="android:paddingStart">@dimen/padding_large_16</item>
    </style>

    <style name="BlueButton">
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:background">@drawable/bg_blue_button</item>
    </style>

    <style name="Description">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/lightGray</item>
    </style>

    <style name="ContactItemName">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/almostBlack</item>
        <item name="android:ellipsize">end</item>
    </style>

</resources>
